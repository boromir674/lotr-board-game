package game.boardGame.components.cards;

import enums.ActivityCardColour;
import enums.ActivityCardType;

public class HobbitCard extends ActivityCard {
	
	public HobbitCard(ActivityCardType type, ActivityCardColour colour) {
		super(type, colour, 1);
	}
	
}
